name: CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

env:
  DOCKERHUB_REPO: 'hamzas-portfolio' #Update this if your Docker Hub repo name is different

jobs: 
  test-and-lint:
    name: Test & Lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: 'services/backend/package-lock.json'

      - name: Install Dependencies
        working-directory: ./services/backend
        run: npm ci

      - name: Run Tests
        working-directory: ./services/backend
        run: npm test --if-present

  build-and-push:
    name: Build & Push Docker Images
    needs: test-and-lint
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build & Push Backend Image
        run: |
          IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/${{ env.DOCKERHUB_REPO }}-backend
          docker build -t $IMAGE:${{ github.sha }} -t $IMAGE:v1.0.4 ./services/backend
          docker push $IMAGE:${{ github.sha }}
          docker push $IMAGE:latest

      - name: Build & Push Frontend Image
        run: |
          IMAGE=${{ secrets.DOCKERHUB_USERNAME }}/${{ env.DOCKERHUB_REPO }}-frontend
          docker build -t $IMAGE:${{ github.sha }} -t $IMAGE:v1.0.4 ./services/frontend
          docker push $IMAGE:${{ github.sha }}
          docker push $IMAGE:latest

      - name: Install DigitalOcean CLI (doctl)
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Trigger App Platform Deployment
        run: |
          APP_ID="${{ secrets.DO_APP_ID }}"
          if [ -z "$APP_ID" ]; then
            echo "Error: DO_APP_ID secret is not set!"
            exit 1
          fi
          echo "Starting deployment for App ID: $APP_ID"
          doctl apps create-deployment "$APP_ID" --wait

      - name: Record Deployment for DORA Metrics
        if: success()
        env:
          DORA_URL: ${{ secrets.DORA_API_URL }}
          DORA_KEY: ${{ secrets.DORA_API_KEY }}
        run: |
          if [ -n "$DORA_URL" ]; then
            TIMESTAMP=$(date -u +"%Y-%m-%dT%H:%M:%SZ")
            curl -sS -X POST "$DORA_URL" \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer $DORA_KEY" \
              -d "{\"repo\":\"${{ github.repository }}\",\"commit\":\"${{ github.sha }}\",\"deployedAt\":\"$TIMESTAMP\"}"
            echo "Deployment recorded successfully."
          else
            echo "DORA_API_URL not set, skipping metric update."
          fi
